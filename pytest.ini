[pytest]
minversion = 6.0
addopts = --cov=pyXLMS --no-cov --cov-fail-under=80
pythonpath = .
testpaths =
    tests
filterwarnings =
    error
    ignore::pyparsing.warnings.PyparsingDeprecationWarning
